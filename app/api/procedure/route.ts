import {NextResponse as R} from "next/server";export const runtime="nodejs";
export async function POST(req:Request){const f=await req.formData();const c=String(f.get("cup_code")||"").trim().toUpperCase();const t=String(f.get("titolo")||"").trim();const p=!c?"/programmazione/progetti/cup":!t?`/programmazione/progetti/${encodeURIComponent(c)}`:`/affidamento/procedure/${Date.now()}`;return R.redirect(new URL(p,req.url),303);}
